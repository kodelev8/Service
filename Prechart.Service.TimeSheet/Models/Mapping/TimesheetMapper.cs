using AutoMapper;

namespace Prechart.Service.TimeSheet.Models.Mapping;

public class TimesheetMapper: Profile
{
    // public TimesheetMapper()
    // {
    //     CreateMap<TimeRecord, TimeRecordEvent>()
    //         .ForMember(d => d.EmployeeId, o => o.MapFrom(s => s.EmployeeId))
    //         .ForMember(d => d.RecordDate, o => o.MapFrom(s => s.RecordDate))
    //         .ForMember(d => d.RecordType, o => o.MapFrom(s => s.RecordType));
    // }
}